{
    "description": "Adds a new $singular to the system.",
    "tags": ["$plural"],
    "parameters": [
        {
            "name": "$singular",
            "in": "body",
            "description": "The $singular JSON you want to post",
            "schema": {
                "$ref": "#/definitions/$resource"
            },
            "required": true
        }
    ],
    "responses": {
        "200": {
            "description": "A new $singular has been created"
        }
    }
}