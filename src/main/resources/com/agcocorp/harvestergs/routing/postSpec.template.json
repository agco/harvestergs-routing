{
    "description": "Adds a $singular to the system.",
    "tags": ["$plural"],
    "parameters": [
        {
            "name": "$singular",
            "in": "body",
            "description": "The $singular JSON you want to post",
            "schema": {
                "$ref": "#/definitions/$resource"
            },
            "required": true
        }
    ],
    "responses": {
        "200": {
            "description": "Make a new $singular"
        }
    }
}