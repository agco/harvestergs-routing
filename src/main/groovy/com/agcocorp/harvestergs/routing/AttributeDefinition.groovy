package com.agcocorp.harvestergs.routing

class AttributeDefinition extends PropertyDefinition {
    AttributeDefinition(String type, Closure cl = null, itemType = null) {
        super(type, cl, itemType)
    }
}
